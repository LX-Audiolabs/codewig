# Polymer — insert + notes OK. Params deferred (filter/osc/env types vary).
# Later: only always-fixed params go here.

id: polymer
bitwig_name: "Polymer"
kind: bitwig
path_hint:
  windows: "%USERPROFILE%/Documents/Bitwig Studio/Library/Devices"
  linux: "~/Bitwig Studio/Library/Devices"
aliases:
  - "poly"
  - "Polymer"
params: {}
